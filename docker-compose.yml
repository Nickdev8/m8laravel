services:
  app:
    build: .
    container_name: m8laravel
    ports:
      - "127.0.0.1:3025:8000"
    volumes:
      - .:/app
      - /app/vendor
      - /app/node_modules
    environment:
      APP_ENV: local
      APP_DEBUG: true
      DB_CONNECTION: sqlite